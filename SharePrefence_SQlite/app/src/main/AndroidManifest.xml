<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.belajarsqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BelajarSQlite">
        <activity android:name=".MainActivity" android:label="Data Mahasiswa">

        </activity>
        <activity
            android:name=".BuatBiodata"
            android:label="Form Biodata Baru" />
        <activity
            android:name=".LihatBiodata"
            android:label="Lihat Biodata" />
        <activity
            android:name=".UpdateBiodata"
            android:label="Update Biodata" />
        <activity android:name=".LoginActivity" android:label="Login" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".RegisterActivity" android:label="Register"/>
    </application>

</manifest>